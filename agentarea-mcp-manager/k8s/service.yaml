apiVersion: v1
kind: Service
metadata:
  name: mcp-manager
  namespace: default
  labels:
    app: mcp-manager
    component: service
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: http
    protocol: TCP
  selector:
    app: mcp-manager
    component: api-server