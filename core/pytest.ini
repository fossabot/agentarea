[tool:pytest]
addopts = -v -s --tb=short
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
norecursedirs = libs .git .venv
markers =
    integration: marks tests as integration tests (requiring real services)
    slow: marks tests as slow running